<?php 


class enum {}
interface enum {}
trait enum {}
enum enum {}
function enum(): enum {}

class from {}
interface from {}
trait from {}
enum from {}
function from(): from {}

from();
enum();

frOm();
eNum();

Enum::foo();
From::foo();

enum::foo();
from::foo();

$a instanceof Enum;
$a instanceof FroM;

$a instanceof enum;
$a instanceof from;

Enum::class;
From::class;
enum::class;
from::class;

from: 12;

goto from;

enum: 123;

goto enum;